---
description: Validate robot behavior with wpilib-agent-tools before finalizing changes.
---

Use `{{RUNNER_PATH}}` to validate the current robot workspace. Start by discovering the available commands with `{{RUNNER_PATH}} --help` and then use the relevant sandbox/sim/log/query flow for the task at hand. Prefer bounded output and gather concrete evidence before claiming completion.
